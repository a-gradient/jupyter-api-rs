[project]
name = "jupyter-shell"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
  "requests>=2.32.5",
]

[dependency-groups]
dev = [
  "jupyterlab>=4.5.1",
]
